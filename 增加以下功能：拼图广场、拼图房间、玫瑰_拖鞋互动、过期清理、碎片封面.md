# 增加以下功能：拼图广场、拼图房间、玫瑰/拖鞋互动、过期清理、碎片封面

请帮我为现有的图片华容道拼图游戏增加以下功能：**拼图广场、拼图房间、玫瑰/拖鞋互动、过期清理、碎片封面**。所有功能在**无账号体系**下实现，但需要为将来接入账号体系**提前埋好扩展点**。

---

## 一、现有表结构（必须严格对齐）

```plaintext
create table public.puzzles (
  id text not null,
  image_url text not null,
  grid_size integer not null,
  moves text not null,
  hidden_index integer not null,
  created_at text not null default ((now() AT TIME ZONE 'utc'::text))::text,
  name text not null default 'HuaRongImage'::text,
  last_opened_at text null,
  cover_fragment_url text null,
  published_to_square boolean default false,
  rose_count integer default 0,
  slipper_count integer default 0,
  open_count integer default 0,
  complete_count integer default 0,
  device_id text null,
  user_id uuid null,
  constraint puzzles_pkey primary key (id)
);
```

### 字段说明

*   `id`：text 类型，用 `gen_random_uuid()`生成
    
*   `grid_size`：对应难度（3,4,5,6,7）
    
*   `moves`：打乱顺序的 JSON 字符串
    
*   `created_at`和 `last_opened_at`：text 类型，用 ISO 字符串格式
    
*   所有新增字段（`cover_fragment_url`等）在插入时必须提供
    

---

## 二、拼图广场（Square）

### 2.1 发布到广场

*   创建拼图时，增加一个复选框：`☐ 发布到拼图广场`
    
*   默认不勾选
    
*   勾选后，`published_to_square`设为 `true`
    
*   只有 `open_count >= 3`或 `complete_count >= 1`的拼图才可发布到广场（防垃圾）
    

### 2.2 广场封面：不展示原图缩略图

*   **广场卡片不展示原图缩略图**，改为展示**拼图碎片**作为封面
    
*   每个拼图在创建时，用 Canvas 从原图**中心区域**随机截取一块大小为 `原图宽/grid_size × 原图高/grid_size`的碎片
    
*   将这块碎片上传到 Cloudinary，URL 存入 `cover_fragment_url`字段
    
*   广场卡片展示这个碎片，而不是原图
    
*   碎片尺寸固定为 200×200px，居中显示
    

### 2.3 广场展示

*   新增页面 `/square`
    
*   展示所有 `published_to_square = true`且未过期的拼图
    
*   每个拼图卡片显示：碎片封面、拼图名称、难度、玫瑰数、拖鞋数、完成次数
    
*   **排序规则**：按 `rose_count`降序 → 按 `created_at`降序（玫瑰数相同的，新的在前）
    

### 2.4 过期移出广场

*   拼图超过 30 天未被打开（`last_opened_at`），自动将 `published_to_square`设为 `false`
    
*   数据不删除，原链接仍可访问，只是不再出现在广场中
    
*   复用已有的清理定时任务
    

---

## 三、拼图房间（Room）

### 3.1 房间是什么

*   房间是一个**多人共享的拼图集合**，存储在 Supabase 后端
    
*   拥有房间链接的任何人都可以访问和添加拼图
    
*   **不需要账号**
    

### 3.2 新增数据表

```plaintext
CREATE TABLE rooms (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  name text DEFAULT '未命名房间',
  owner_id uuid,                    -- 预留：将来关联 users.id，现在可为 NULL
  device_id text,                   -- 当前创建设备标识
  created_at text DEFAULT (now() AT TIME ZONE 'utc')::text
);

CREATE TABLE room_puzzles (
  id text PRIMARY KEY DEFAULT gen_random_uuid()::text,
  room_id text REFERENCES rooms(id) ON DELETE CASCADE,
  puzzle_id text REFERENCES puzzles(id) ON DELETE CASCADE,
  added_by text DEFAULT '匿名',
  added_by_user_id uuid,            -- 预留：将来关联 users.id
  added_by_device_id text,          -- 当前添加者设备标识
  created_at text DEFAULT (now() AT TIME ZONE 'utc')::text,
  UNIQUE(room_id, puzzle_id)
);
```

### 3.3 房间功能

**创建房间**

*   页面增加"创建房间"按钮
    
*   用户输入房间名称（可选，默认"未命名房间"）
    
*   调用 `POST /api/rooms`，body: `{ name, deviceId }`
    
*   返回 `roomId`
    
*   生成房间链接：`/room/{roomId}`
    
*   用户可复制链接分享给朋友
    

**查看房间**

*   打开 `/room/{roomId}`
    
*   调用 `GET /api/rooms/:id`，返回房间信息 + 拼图列表
    
*   拼图按添加时间正序展示
    
*   过期的拼图显示"🕰 已过期"标记，但仍保留在列表中
    

**添加拼图到房间**

*   在拼图页面增加"添加到房间"按钮
    
*   用户输入房间 ID 或从已加入的房间列表中选择
    
*   调用 `POST /api/rooms/:id/puzzles`，body: `{ puzzleId, deviceId }`
    
*   成功后该房间所有成员可见
    

**从房间移除拼图**

*   房间内每个拼图有"移除"按钮
    
*   调用 `DELETE /api/rooms/:id/puzzles/:puzzleId`
    

### 3.4 房间权限

*   初期规则：**拥有房间链接的任何人都可以添加和移除拼图**
    
*   房间页面显示当前拼图总数
    

---

## 四、玫瑰 / 拖鞋互动

### 4.1 功能描述

*   在拼图详情页增加两个按钮：🌹 送玫瑰、🩴 扔拖鞋
    
*   点击后调用接口记录，计数实时更新
    
*   仅用于氛围，不绑定用户身份
    

### 4.2 接口

```plaintext
POST /api/puzzle/:id/react
headers: { 'x-device-id': deviceId }
body: { type: "rose" | "slipper" }
```

后端处理：

```plaintext
UPDATE puzzles SET 
  rose_count = CASE WHEN type = 'rose' THEN rose_count + 1 ELSE rose_count END,
  slipper_count = CASE WHEN type = 'slipper' THEN slipper_count + 1 ELSE slipper_count END
WHERE id = :puzzleId;
```

### 4.3 防刷机制（简单有效）

*   前端：用 `localStorage`记录已反应的拼图 ID 和类型，同设备同拼图同类型只能反应一次
    
*   后端：按 `deviceId`做每日频率限制（可选）
    

---

## 五、接口清单（新增/修改）

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/api/rooms` | 创建房间，body: `{ name, deviceId }`，返回 `{ id, name }` |
| GET | `/api/rooms/:id` | 获取房间信息 + 拼图列表（含每个拼图的过期状态） |
| POST | `/api/rooms/:id/puzzles` | 向房间添加拼图，body: `{ puzzleId, deviceId }` |
| DELETE | `/api/rooms/:id/puzzles/:puzzleId` | 从房间移除拼图 |
| GET | `/api/square` | 获取广场拼图列表（玫瑰降序 + 时间降序，不含过期） |
| POST | `/api/puzzle/:id/react` | 送玫瑰或扔拖鞋，body: `{ type }`，header 带 deviceId |

---

## 六、前端页面变更

### 新增页面

*   `/square`：拼图广场（卡片展示碎片封面）
    
*   `/room/:id`：拼图房间
    

### 现有页面修改

*   **创建拼图页**：增加"发布到广场"复选框；生成拼图时截取碎片并上传 Cloudinary；所有请求携带 `deviceId`
    
*   **拼图详情页**：增加"添加到房间"按钮 + 玫瑰/拖鞋按钮
    
*   **导航栏**：增加"广场"和"我的房间"入口
    

### 前端 deviceId 管理

在 `index.html`的 `<script>`起始处加入：

```plaintext
const DEVICE_ID_KEY = 'puzzle_device_id';
let deviceId = localStorage.getItem(DEVICE_ID_KEY);
if (!deviceId) {
  deviceId = crypto.randomUUID();
  localStorage.setItem(DEVICE_ID_KEY, deviceId);
}
// 所有 fetch 请求自动带上 header
const originalFetch = window.fetch;
window.fetch = function(url, options = {}) {
  options.headers = options.headers || {};
  options.headers['x-device-id'] = deviceId;
  return originalFetch.call(this, url, options);
};
```
---

## 七、提前埋点：为将来接入账号做准备

### 7.1 数据库预留字段

所有表中均已预留 `user_id`和 `device_id`字段，匿名期间 `user_id`为 NULL，`device_id`必填。

### 7.2 接口兼容设计

*   所有需要身份的操作（创建拼图、创建房间、添加记录、送花等）**同时支持**：
    
    *   匿名：通过 `x-device-id`header 标识设备
        
    *   登录后：通过 `Authorization: Bearer <token>`标识用户
        
*   后端统一处理逻辑：
    

```plaintext
const userId = req.user?.id;
const deviceId = req.headers['x-device-id'];
```

### 7.3 匿名数据迁移接口（预留，暂不实现）

将来加账号后，可通过 `deviceId`将匿名期间的数据归属到用户：

```plaintext
POST /api/user/migrate-anonymous-data
body: { deviceId }
```
---

## 八、过期清理逻辑

在清理定时任务中增加：

```plaintext
UPDATE puzzles 
SET published_to_square = false 
WHERE published_to_square = true 
  AND last_opened_at < (now() AT TIME ZONE 'utc')::text - INTERVAL '30 days';
```

注意：`last_opened_at`是 text 类型，需确保存储的是 ISO 格式字符串，比较时用 `::timestamp`转换：

```plaintext
UPDATE puzzles 
SET published_to_square = false 
WHERE published_to_square = true 
  AND last_opened_at IS NOT NULL
  AND last_opened_at::timestamp < NOW() - INTERVAL '30 days';
```
---

## 九、期望输出

在现有项目基础上增量修改，不要破坏已有功能。请确保：

*   所有新增接口有基本的错误处理
    
*   前端页面适配移动端
    
*   玫瑰/拖鞋计数实时更新（无需刷新页面）
    
*   广场卡片展示碎片封面，不展示原图缩略图
    
*   前端自动管理 `deviceId`，所有请求携带 `x-device-id`header
    
*   数据库操作严格对齐现有 `puzzles`表结构
    
*   新增的 `rooms`和 `room_puzzles`表使用与 `puzzles`表一致的 text 类型主键和时间格式